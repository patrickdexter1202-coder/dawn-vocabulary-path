# 晨光词径 Web 设计与质量 QA

当前规格：v0.9
更新日期：2026-07-03

## v0.2 功能扩展（2026-06-30）

- [x] 保留晨光词径既有视觉语言。
- [x] 新增词库选择，不引入另一套导航结构。
- [x] 默写卡片沿用玻璃、蓝色和圆角组件语言。
- [x] 跟读、默写、结果、记录、错词清单均可交互。
- [x] 六年级教材版本和单元标签在学习页可见。
- [x] 新控件和弹窗包含桌面、平板、手机响应式规则。
- [ ] v0.2 最终浏览器截图待刷新。

## v0.3 每日会话（2026-06-30）

- [x] 开始页沿用原三栏布局与玻璃卡片语言。
- [x] 顶部统计区同时区分本次与今日。
- [x] 默写时答案区以模糊、低透明度和覆盖卡三重遮蔽。
- [x] 词库箭头改为内置图标，并保留足够右边距。
- [x] 学习记录按今日、会话、错误明细建立层级。
- [x] 手机布局将每日统计压缩为横向三格，开始与完成操作纵向排列。

## v0.4 记录与排版（2026-06-30）

- [x] 长单词按长度切换两档紧凑字号，并允许安全换行。
- [x] 主播放按钮缩小，词条与操作区边界更清楚。
- [x] 学习记录首层只保留答对、答错；逐词结果在会话内展开。
- [x] 正确/错误逐词状态使用绿/棕红辅助辨识，并保留文字标签。
- [x] 删除会话和错词均经过独立的家长验证弹窗。
- [x] 密码错误状态有可访问提示，危险操作与取消操作视觉区分。
- [x] 浏览器实测修正密码弹窗层级：密码层 `z-index: 40`，高于记录层 `30`。

- 验收日期：2026-06-29
- 验收范围：`app/` 响应式学习页
- 视觉基准：`app/design/reference-desktop-1440x1024.png`

## 自动验证

- 自动化测试：5 个测试文件、43 个测试全部通过。
- `pnpm build:pages`：Vite 生产构建和项目子路径断言成功。
- `pnpm audit --prod`：无已知生产依赖漏洞。
- 浏览器控制台：本地桌面/平板/手机和正式 GitHub Pages 验收期间无资源加载错误。

## 响应式与视觉检查

| 视口 | 结果 | 备注 |
| --- | --- | --- |
| 1440 × 1024 | 通过 | 三列构图与视觉基准一致；页面宽高均无溢出。 |
| 820 × 1180 | 通过 | 两列布局完整；播放区与操作区保持清晰层级。 |
| 390 × 844 | 通过 | 今日会话与作答摘要双列同高；词条和操作区纵向排列；无横向溢出。 |

桌面端已对照检查字体、间距、渐变、图标、文案和视觉层级。现有实现保留了基准稿的晨光渐变、四轮学习路径、中心词条和右侧跟读操作，未发现 P0、P1 或 P2 视觉差异。

## 交互与可访问性检查

- 个人菜单可展开与收起，`aria-expanded` 状态正确。
- 1.0 倍与 0.75 倍速度可切换，`aria-pressed` 状态正确。
- “我跟读好了”可从 `journey` 进入 `practice`，进度由 `12/30` 更新为 `13/30`。
- 会话结束、回车操作、动态选词、在线音频优先和本地语音回退已有自动化测试覆盖。
- 关键按钮具备可访问名称、键盘焦点样式和至少 44px 的触控目标。
- 支持 `prefers-reduced-motion`，核心文字对比度满足当前视觉基准。

## v0.7 在线网页与移动端检查（2026-07-02）

- [x] 有道在线英音地址正确编码词语与空格。
- [x] 在线播放支持 1.0 倍与 0.75 倍速度。
- [x] 在线播放拒绝或失败时只回退一次到 `speechSynthesis`。
- [x] 两种语音都失败时显示明确提示。
- [ ] 真实断网环境下人工验证英文系统语音回退。
- [x] 已设计 `920px`、`680px`、`390px` 响应式断点，并完成 390×844 视口检查。
- [ ] 真实 iPhone Safari 的音频、键盘、安全区和存储生命周期验收。
- [x] GitHub Pages 项目子路径、字体和静态资源线上验收。
- [ ] 自定义词库导入页面的键盘可访问性、错误摘要和大文件反馈。

## v0.8 本地数据与正式发布检查（2026-07-02）

- [x] 正式站点包含“衔接混合”542 条、“小学基准”498 条和“六年级新词”44 条。
- [x] CSV 支持 UTF-8、引号内逗号与换行；JSON 支持数组或 `entries`；XLSX 支持多 Sheet。
- [x] 导入页提供四个核心字段映射、前 20 行预览、重复策略和结果报告。
- [x] IndexedDB 事务保存、归档、刷新读取和 1,000 词唯一 ID 测试通过。
- [x] 完整备份同时包含 v2 学习记录和自定义词库；损坏版本被拒绝。
- [x] 正式站点完成开始学习、进入默写、正确提交和新标签页重开持久化验收。
- [x] GitHub Actions 测试、构建、artifact 与 Pages 部署成功。
- [ ] 接近 10 MB/20,000 行的大文件人工性能验收。
- [ ] 真实设备执行一次备份导出、清空与恢复演练。

## v0.8 移动端密度与加载优化（2026-07-02）

- [x] 390×844 下“今日学习/会话”与“今日作答/词数”并排显示，卡片均为 112px 高。
- [x] 答对、答错、提示统计缩短上下间距，并保持三个等宽触控区。
- [x] 个人菜单“本地词库与备份”保持单行，390px 视口无溢出。
- [x] 桌面数据页三个文件操作按钮顶部均为 420px、高度均为 48px。
- [x] 品牌字体由 5,052,644 B 完整 TTF 改为 533,872 B WOFF2 子集，生产包减少约 89.4%。
- [x] 本地浏览器检查无控制台警告或错误。

final result: passed

## v0.9 近 90 天学习记录（2026-07-03）

- [x] 学习记录默认显示当天，并显示当天答对、答错和会话明细。
- [x] 前一天、后一天和原生日历输入均可切换查询日期。
- [x] 查询范围为当天及之前 89 天；未来日期和范围外日期不可选。
- [x] 日期切换会收起旧会话，空日期显示明确空状态。
- [x] 日期导航按钮保持至少 44px 触控尺寸，390px 视口使用三列紧凑布局。
- [x] 页面明确说明记录只保存在当前浏览器，不上传 GitHub。

## v0.9.1 移动端记录日期导航（2026-07-06）

- [x] 参考图：用户提供的 iPhone 截图，原生日期输入框覆盖“后一天”按钮。
- [x] 目标状态：390 × 844，打开学习记录弹窗并显示默认日期。
- [x] 日期标签占满并裁切中间网格；原生日期输入明确使用 `min-width: 0` 与 `max-width: 100%`。
- [x] 前一天、后一天仍是独立的 44px 触控区；视觉语言和近 90 天查询逻辑不变。
- [x] CSS 回归测试、全量测试和 GitHub Pages 生产构建均通过。
- [ ] 内置浏览器插件的 `incrementalAriaSnapshot`/定位接口超时阻断了最终截图；仍需在真实 iPhone Safari 刷新确认。

保真检查：除日期导航的边界约束外，布局、间距、字体、颜色与组件样式均未改动；未发现 P0/P1/P2 代码级回归。

final result: passed with real-device Safari confirmation pending
